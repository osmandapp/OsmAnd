package net.osmand.plus.base.dialog.interfaces.dialog;

/**
 * Used to indicate all types of interactions with dialog and controller.
 * Extend this each time when creating a new type of interaction that should be processed in a dialog.
 */
public interface IDialog { }
