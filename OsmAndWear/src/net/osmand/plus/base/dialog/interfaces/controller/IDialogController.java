package net.osmand.plus.base.dialog.interfaces.controller;

/**
 * Used to indicate all types of interactions with controller and dialog.
 * Extend this each time when creating a new type of interaction that should be processed in a controller.
 */
public interface IDialogController { }