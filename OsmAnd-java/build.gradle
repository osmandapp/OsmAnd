apply plugin: 'java'

sourceSets {
	main {
		java {
			srcDirs = [
					"src"
			]
		}
	}
}

// This is Android-specific
jar {
	exclude "**/PlatformUtil.*"
}

dependencies {
	compile fileTree(dir: "libs", include: ["*.jar"])
}
